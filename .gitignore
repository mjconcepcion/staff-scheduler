# Real data lives here — never commit it. Use "Load sample data" in the app instead.
*.db
*.db-journal

# Environment / caches
.venv/
__pycache__/
*.pyc
.streamlit/secrets.toml

# Local agent instructions — functions from the filesystem, stays out of the public repo

# Editor noise
.vscode/
.idea/
